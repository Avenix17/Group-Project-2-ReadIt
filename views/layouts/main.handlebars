<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ReadIt</title>
  <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@400;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://unpkg.com/bulma@0.9.0/css/bulma.min.css" />
  <link rel="stylesheet" href="css/style.css"/>
  <link rel="stylesheet" href="css/login.css"/>
  <link rel="stylesheet" href="css/homepage.css"/>
  <link rel="stylesheet" href="css/searchResult.css"/> 
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
  <link href="https://fonts.googleapis.com/css?family=Poppins:600" rel="stylesheet">
  
</head>

<body>
    <div>
        <header>
    
          <nav>
        <!-- Conditionally render logout link -->
        {{#if logged_in}}
        <div class="navbar-start" style="background-color: rgb(255, 247, 209)">
          <h1 class="title" style="padding-top: 25px; margin-left: 50px"><i class="bi bi-book"></i> ReadIt</h1>
        
          <div class="navbar-end">
            <span class="navbar-item">
              <a class="button is-black is-outlined" id="logout">
                <span>Logout</span>
              </a>
            </span>
          </div>
        </div>
        {{/if}}
      </nav>

      </header>
      <main>
        {{{body}}}
      </main>

      <footer>
        <!-- not sure if adding footer -->
      </footer>
    </div>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
</html>